mixin button(text, type="primary", href)
  - var className = `button button--${type}`
  if href
    a(class= className href= href)
      span.button__wrapper= text
  else
    button(class= className type="button")&attributes(attributes)
      span.button__wrapper= text